{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "HTTP or HTTPS URL to fetch."
    },
    "max_length": {
      "type": "integer",
      "description": "Maximum number of characters to return (default 5000, max 20000).",
      "default": 5000,
      "maximum": 20000
    },
    "start_index": {
      "type": "integer",
      "description": "Character offset for pagination (default 0).",
      "default": 0,
      "minimum": 0
    },
    "raw": {
      "type": "boolean",
      "description": "When true, return raw HTML instead of Markdown.",
      "default": false
    }
  },
  "required": ["url"]
}
