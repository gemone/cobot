{
  "type": "object",
  "properties": {
    "enabled_mcp": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of MCP server names to enable"
    },
    "enabled_skills": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of skill names to enable"
    },
    "sandbox_root": {
      "type": "string",
      "description": "Sandbox root directory"
    },
    "allow_paths": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Additional allowed paths"
    },
    "readonly_paths": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Additional readable but not writable paths"
    },
    "allow_network": {
      "type": "boolean",
      "description": "Whether shell_exec may use network-capable commands"
    },
    "allowed_network_tools": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tools explicitly allowed to access the network when allow_network is true"
    },
    "blocked_commands": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Blocked shell commands"
    }
  }
}
