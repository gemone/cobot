{
  "type": "object",
  "properties": {
    "prompt": {
      "type": "string",
      "description": "The task prompt for the sub-agent"
    },
    "model": {
      "type": "string",
      "description": "Optional model override in provider:model format. Omit to inherit the current model."
    }
  },
  "required": ["prompt"]
}
